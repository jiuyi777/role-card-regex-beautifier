# 修改记录

## 2026-07-24：更正为手机关闭按钮

- 用户原话：上一轮说错了，问题是手机端关闭按钮，不是删除按钮。
- 当前理解：弹窗右上角关闭键在手机上被标题挤压、位置不清楚或不可见；固定指令删除键不是本轮问题。
- 删除或重写的旧逻辑：撤回上一轮新增的手机整行文字删除键；把关闭键从普通 Header Flex 子项改成受弹窗标题栏约束的右上角固定锚点。
- 新目标与不修改范围：目标为 `.rcra-header` 与 `.rcra-close`，手机端始终位于右上角且有清晰底色、边框和点击范围；不修改预设选择、流式生成、固定指令数据及其他布局。

## 2026-07-24：手机删除入口、酒馆预设与流式生成

- 用户原话：手机端看不清或找不到删除按键；预设文件不能正常导入，希望直接选择酒馆原来已有的预设；生成需要流式传输。
- 当前理解：每条固定指令在手机端必须显示独立文字删除键；插件不再维护需要导入的自定义预设文件，改为读取当前接口的 SillyTavern 生成预设列表；AI 返回内容实时写入结果框。
- 删除或重写的旧逻辑：删除合并在风格预设区的导入、新建、导出、删除与编辑逻辑；重写非流式 `generateRaw` 主路径；重写手机端仅显示小垃圾桶图标的布局。
- 新目标与不修改范围：目标为 `.rcra-delete-directive`、`#rcra-generation-preset-select`、`generateRegex()` 和 `@media (max-width: 430px)`；不修改角色卡读取、参考正则母版、固定指令数据、结果安全扫描和保存到角色正则的逻辑。
- 版本证据：只读核对 `D:\SillyTavern\package.json`，当前版本为 `1.14.0`；流式接口来自该版本 `ChatCompletionService` / `TextCompletionService`。
